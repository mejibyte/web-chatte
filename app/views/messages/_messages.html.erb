<% @messages.each do |message| %>
  <%= div_for message do %>
    <span><%= l message.created_at, :format => "[%I:%M%p]" %> </span><b><%= message.from %> says:</b> <span><%= message.content %></span>
  <% end %>
<% end %>
